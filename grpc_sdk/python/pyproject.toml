[tool.ruff]
line-length = 120
select = [
    "B",
    "C",
    # Pyflakes
    "F",
    # Pycodestyle
    "E",
    "W",
    # isort
    "I001",
    # Other
    "PT",
    "SIM",
    "RUF",
    "ERA",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
